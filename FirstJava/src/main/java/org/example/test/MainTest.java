package org.example.test;

public class MainTest {
    // [아무것도 입력하지 않으면 default(같은 패키지면 됩니다)], public(누구나 됩니다.), private(나만 됩니다.), protected(내 상속관계만 됩니다. 같은 패키지까지 됩니다.)

    // 필드 (멤버 변수, 속성 )
    private double a;
    String b;

    // 메서드 (멤버 함수, 행동 )
    // 반환값 메서드 이름 ( 입력 값 )
    public void clear(){

    }

    double calculate(int number, int number2){
        return 0.0f;
    }

    boolean isEmpty(){
        return false;
    }
}
